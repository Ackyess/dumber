# DUMBER Privacy Notice

最后更新：2026-07-23

## 1. DUMBER 处理什么数据

启用后，DUMBER 会读取接近当前视口的 X 卡片中可见的有限上下文，例如正文、作者、引用内容、链接标题和可见话题。

这些字段会被发送到用户在设置页自行配置的模型 API，用于生成结构化策展结果。

## 2. DUMBER 不处理什么数据

当前版本不会主动抓取：

- 完整浏览历史；
- 页面外不可见的内容列表；
- X 私信；
- 浏览器账号、密码或表单内容；
- 与策展无关的页面正文。

## 3. 本地存储

DUMBER 使用 `chrome.storage.local` 保存：

- API 地址、模型和 API Key；
- 开关与推广阈值；
- 结构化判定缓存；
- `更多这种 / 这条真有用` 偏好画像；
- 聚合工程指标。

`chrome.storage.session` 还保存同一浏览器会话内的判定热缓存。

判定缓存只保存版本化 hash 和结构化结果，不保存原始卡片文本。

## 4. API Key

API Key：

- 不包含在源码；
- 不包含在本地数据导出文件；
- 不上传到 DUMBER 自有服务器；
- 只用于向用户配置的 API origin 发起授权请求。

浏览器配置文件或设备被控制时，本地 Key 仍可能被读取。用户应使用可撤销、额度受限的个人 Key。

## 5. 网络与权限

DUMBER 的内容脚本只在清单声明的 X 页面运行。仓库中保留的 Bilibili 适配器源码不在当前 Manifest 脚本列表中，也没有对应站点权限。

模型 API host permission 是可选权限。用户保存或测试 API 配置时，Chrome 会针对该 origin 请求授权。DUMBER 不要求安装时获得所有网站的网络访问权。

扩展会在请求前检查该 origin 的授权状态。缺少授权时不会尝试发送卡片 context；popup 会显示需要恢复 API 访问权限。

远程 API 必须使用 HTTPS。HTTP 只允许 `localhost` 与 `127.0.0.1`，用于用户主动配置的本机模型服务；包含 URL 用户名或密码的地址会被拒绝。

DUMBER 不加载远程 JavaScript，不使用远程托管代码。

## 6. 遥测与账户

DUMBER 没有：

- DUMBER 云端账户；
- 云同步；
- 第三方分析 SDK；
- 广告追踪；
- 后台遥测；
- 订阅或支付系统。

popup 中的统计来自本地聚合指标。

## 7. 导出与删除

设置页提供：

- 导出本地配置摘要、偏好画像和工程指标；
- 清除判定缓存、偏好画像和工程指标。

清除操作保留 API 配置和总开关，避免意外删除用户凭据。删除扩展会由 Chrome 删除该扩展的本地存储。

执行清除时，DUMBER 会中止正在进行的模型请求、清空等待队列，并在缓存、偏好和指标写入完成后写入空状态，避免旧响应在清除后重新生成本地数据。

## 8. 第三方模型服务

用户配置的 API 服务会按照其自身隐私政策处理发送内容。DUMBER 无法控制第三方服务的保留、训练或审计策略。用户应在使用前审查所选服务条款。
