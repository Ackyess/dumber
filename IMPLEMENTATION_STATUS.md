# Roadmap Implementation Status

本文件记录 0.3.1 对 `ROADMAP.md` 的实现覆盖，不修改路线图本身的验收定义。

## v0.2 — 运行时重置与稳定性

已实现：

- 运行时旧语义清理；
- 新模型 schema；
- X/Bilibili 独立适配器；
- 稳定内容 ID 与指纹；
- 后台单飞批处理；
- pending/active 同键合并与运行 generation 隔离；
- 分阶段超时、一次网络重试与一次卡片级延迟重试；
- 未配置时零扫描、API 权限状态检测与网络前置拦截；
- 完整版本化缓存键；
- 清除数据时的活动请求中止与写入屏障；
- Shadow DOM、SPA 和虚拟列表状态修复。

仍需真实站点长时间验收：

- 连续滚动 30 分钟；
- 缓存恢复 100 ms；
- 新判定 p95 4 秒。

仓库提供指标采集与 Chromium fixture，但不会以本地 fixture 替代真实站点验收。

## v0.3 — VIP 阅读渲染器

已实现：

- 边缘虹彩和环境光；
- 高对比阅读平面；
- 字号、行距、标题展开；
- 次要信息弱化；
- 唯一 sidecar；
- 无重叠位置时隐藏 sidecar；
- 不覆盖宿主 CSS 动画的独立虹彩动画；
- reduced motion；
- light/dark surface 检测。

仍需在真实站点做 WCAG 对比度采样和多主题截图回归。

## v0.4 — 多巴胺策展模型

已实现工程基础：

- 完整内部输出字段；
- X 引用与链接上下文；
- Bilibili 标题、UP 主、标签和简介上下文；
- 提示词版本与回归数据格式；
- 离线评分器。

未完成：

- 300 条真实样本；
- 90% 精确率验收；
- 完整提示词回归报告。

## v0.5 — 真诚的个性化推荐

已实现基础版本：

- `更多这种 / 这条真有用`，并对同一内容保存硬性纠错；
- driver、作者和话题偏好；
- 完全本地存储；
- popup 推广状态和 streak；
- 数据查看摘要、导出和清除。

仍需真实使用数据验证阈值调整是否稳定、是否需要衰减机制。

## v0.9 — 开源 Beta

已实现：

- BYOK；
- 无源码秘密信息；
- 原创 icon，移除第三方 meme；
- 单元测试、DOM fixture、Chromium smoke test；
- 后台 service worker VM 测试与运行代际竞态回归；
- GitHub Actions；
- 可复现 ZIP；
- 隐私、贡献和适配器文档；
- 平台范围保持 X 与 Bilibili。

发布前仍应完成：

- 真实站点长时间集成测试；
- Chrome Web Store 清单与截图；
- 第三方模型兼容矩阵；
- 300 条评测集。
