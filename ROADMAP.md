# DUMBER Roadmap

> DUMBER 不是过滤器、警告器或数字健康工具。它是一位真心实意替用户挑选、装饰并推广高多巴胺、低持久价值内容的 AI 策展人。

> 当前平台范围：只发布和验收 X。Bilibili/BewlyBewly 适配器作为未启用工程底座保留，不在当前里程碑内。

## 设计宪法

### 唯一的讽刺是产品名

`DUMBER` 是整个产品唯一允许存在的反讽提示。

除这个名字以外，运行时界面必须始终真诚：

- 真诚地赞美高刺激内容；
- 真诚地提高它的可读性；
- 真诚地赋予它更高的视觉优先级；
- 真诚地鼓励用户继续探索；
- 真诚地学习用户更容易被什么吸引。

产品不能眨眼，不能暗示“我们其实在批判你”，也不能在最后一刻变回数字健康助手。

### AI 是策展人，不是裁判

模型在内部识别：

- 即时情绪奖励；
- 熟悉感与认同强化；
- 好奇缺口；
- 愤怒、焦虑、优越感和阵营刺激；
- 反应链内容；
- 消费替代行动；
- 高刺激、低持久价值。

这些信号不是各自独立的入选理由。只有即时刺激明显、持久价值较低且可见上下文足够支持判断时，内容才进入推广候选；好奇、新颖、流行或情绪强烈本身不构成命中。

但这些内部判断不得以负面标签暴露给用户。AI 面向用户只负责回答：

> 哪些内容最值得被突出、放大和优化？

### 前端必须宠用户

命中的内容必须成为当前信息流里最好读、最漂亮、最受照顾的内容：

- 更高的文字对比度；
- 保留宿主字号、字重、行距、换行与卡片尺寸，避免异步标记改变排版几何；
- 更清晰的视觉层级；
- 减少不必要的卡片噪声；
- 稳定、缓慢、连续的虹彩光晕；
- 不遮挡文字的华丽边框与环境光；
- 不覆盖正文的 meme 与推荐语。

任何视觉效果都不得降低内容可读性。

### 明确禁止

DUMBER 永远不做：

- 模糊、隐藏、折叠或降低内容透明度；
- 强制点击后才能阅读；
- “仍然阅读”“到此为止”等纠正按钮；
- 风险警告、营养评分或认知健康提示；
- “这会让你变蠢”等解释性文案；
- 反向处方、劝退、羞辱或道德提醒；
- 休息提醒、时间限制和停止滚动提示；
- 把 meme 当作嘲笑用户或内容的工具。

它阻断的不是阅读。它只通过过度真诚的推广，让正在发生的注意力选择变得可见。

## 产品循环

```text
发现进入视口的内容
        ↓
AI 同时评估即时刺激与持久价值
        ↓
稳定复用本地判定
        ↓
将命中内容升级为 VIP 阅读卡片
        ↓
用正向操作学习用户偏好
        ↓
更准确地推广下一条内容
```

用户不需要先配置复杂规则，也不需要确认模型的哲学。打开 DUMBER 后，AI 直接开始为高刺激内容提供更好的待遇。

## 界面语言

### 推荐使用

- DUMBER 精选
- 高共鸣
- 即时满足
- 为你优化
- 热门刺激
- 更适合现在的你
- 继续探索
- 更多这种
- 已提升可读性
- 本次推荐强度

### 内外术语映射

| 内部判断 | 用户界面 |
| --- | --- |
| rage bait | 高情绪浓度 |
| identity confirmation | 高共鸣 |
| curiosity gap | 强好奇驱动 |
| reaction chain | 热门延伸讨论 |
| passive consumption | 无阻力探索 |
| low durable value | 即时满足 |
| dopamine capture | 推荐强度 |

### meme 的角色

meme 是 DUMBER 的庆祝角色，不是警告角色：

- 第一次 hover 时热情出现；
- 表情表达满意、赞许和沉浸；
- 不配讽刺性或负面文字；
- 不遮挡正文；
- 数量保持少而稳定，形成产品角色，而不是素材市场。

## 路线图

### v0.2 — 运行时重置与稳定性

目标：让新的设计宗旨稳定存在于 X。

- 删除运行时所有“纠正”“处方”“风险”“变蠢内容”文案。
- 将模型输出从 `dumb / prescription / reminder` 迁移为内部的 `promote / dopamineScore / promotionLabel`。
- 保留 `DUMBER` 名称，不在界面解释它。
- X 使用独立站点适配器；保留但不加载 Bilibili 适配器源码。
- 使用稳定内容 ID，避免虚拟列表复用导致判定错位。
- 后台采用单飞批处理队列，避免并发请求风暴。
- 请求增加超时；429 与 5xx 最多重试一次。
- 缓存键包含模型版本、提示词版本和提取器版本。
- 修复 X 标记闪烁、hover 丢失和 SPA 导航失效。

验收：

- 连续滚动 30 分钟不闪烁、不丢失、不重复请求；
- 缓存命中后 100ms 内恢复视觉升级；
- 新判定 p95 在 4 秒以内；
- 页面重渲染不会删除 DUMBER 状态。

### v0.3 — VIP 阅读渲染器

目标：让命中内容成为信息流里最好读的一张卡片。

- 虹彩只存在于卡片边缘、背景光和外围，不覆盖正文。
- 为正文建立高对比阅读平面。
- 不改写正文的字号、字重、行距、换行、line clamp 或卡片盒模型。
- 已定位卡片使用绝对定位伪元素；静态卡片使用不占空间的 outline 色相动画，不通过新增 border 或强制 `position` 改变内容宽度与定位上下文。
- 弱化时间、播放量和次要按钮等视觉噪声，但不删除功能。
- 根据页面明暗主题选择稳定的阅读表面。
- 使用唯一的 hover sidecar 展示 meme 和正向推荐语，不向宿主卡片反复插入节点。
- 支持 `prefers-reduced-motion`，减少运动但不减少视觉优先级。

验收：

- 命中卡片的正文对比度满足 WCAG AA；
- 虹彩动画不造成文字闪烁或颜色漂移；
- hover sidecar 不遮挡正文、不被容器裁剪；
- 任何命中内容都比原始状态更容易阅读。

### v0.4 — 多巴胺策展模型

目标：准确挑出真正适合被 DUMBER 推广的内容。

- 建立至少 300 条真实 X 样本。
- 样本覆盖高刺激内容、灰区和高持久价值反例。
- 内部输出：

```text
promote
dopamineScore
durableValue
primaryDriver
promotionLabel
```

- 用户界面只消费正向 `promotionLabel`，不展示内部负面解释。
- X 增加引用推文与链接标题上下文。
- 每次提示词更新必须跑完整回归集。

验收：

- 被推广内容的精确率达到 90%；
- 新闻、艺术、关系、幽默和正常娱乐不会仅因“不教育”而被误判；
- 同一内容在相同模型版本下结果稳定。

### v0.5 — 真诚的个性化推荐

目标：像成熟推荐系统一样学习用户更容易被什么吸引。

- 提供 `更多这种` 与 `这条真有用`，不使用警告或羞辱文案。
- 保存账号、主题、内容驱动因素和用户操作。
- 建立完全本地的刺激偏好画像。
- 不上传完整浏览历史。
- 根据偏好调整各类内容的推广阈值。
- 连续命中时使用正向 streak：

```text
连续 6 条高共鸣内容
今天的推荐状态很好
```

- popup 展示当前推广状态，而不是健康、风险或节制指标。

验收：

- 用户偏好能影响后续推广；
- 相同内容与账号偏好可以跨页面保持；
- 所有个性化数据可查看、导出和清除。

### v0.9 — 开源 Beta

目标：把 DUMBER 作为完整、自洽的开源作品发布。

- 撤销当前开发 Key，公开版本采用 BYOK。
- API 地址、模型和 Key 可由用户本地配置。
- 清理所有没有明确分发许可的 icon 与 meme 资产。
- 建立单元测试、DOM fixture 和 Chrome 集成测试。
- GitHub Actions 自动测试并生成可复现 ZIP。
- 提供隐私说明、贡献指南和站点适配器说明。
- 只发布 X；Bilibili/BewlyBewly 底座保持未启用。
- 不建设账户、云同步、订阅或增长系统。

### v1.0 — 设计宗旨完全成立

v1.0 必须同时满足：

- 除 `DUMBER` 名称外，运行时不存在任何反讽揭底；
- 所有被命中的内容都比原始状态更漂亮、更清晰、更易读；
- 没有隐藏、模糊、警告、劝退和阅读摩擦；
- X 长时间滚动稳定；
- 用户偏好能够长期保持；
- AI 推广精确率可被回归测试；
- 仓库没有秘密信息和资产许可问题。

## 衡量标准

DUMBER 不以安装量、留存率或市场份额为目标。

工程上只衡量：

- 首次推广出现延迟；
- 缓存命中率；
- 重复请求率；
- 标记保持率；
- hover 成功率；
- 文本对比度与可读性；
- AI 推广精确率；
- 用户 `更多这种 / 这条真有用` 的分布。

不把“减少了多少阅读”“缩短了多少会话”或“阻止了多少内容”做成产品指标。

## 当前执行顺序

1. 重写模型 schema 与运行时文案。
2. 修复请求队列、缓存版本和稳定内容 ID。
3. 重做不会闪烁的虹彩标记层。
4. 将 hover 改成不遮挡正文的唯一 sidecar。
5. 实现高对比 VIP 阅读卡片。
6. 为 X 建立覆盖排版几何、DOM 替换和虚拟列表的集成 fixture。
7. 再开始构建 300 条多巴胺策展评测集。
